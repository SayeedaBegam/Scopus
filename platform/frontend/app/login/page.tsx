"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {api,User} from "@/lib/api";
export default function Login(){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=new FormData(e.currentTarget);try{const result=await api<{user:User}>("/auth/login",{method:"POST",body:JSON.stringify({email:data.get("email"),password:data.get("password")})});router.push(result.user.role==="admin"?"/analytics":"/professors")}catch(e){setError(e instanceof Error?e.message:"Unable to sign in")}finally{setBusy(false)}}return <main className="min-h-screen grid lg:grid-cols-2"><section className="hidden lg:flex bg-utn-navy text-white p-16 flex-col justify-between"><div className="font-bold text-2xl">UTN</div><div><p className="uppercase tracking-[.25em] text-teal-200 text-sm">Research without borders</p><h1 className="text-5xl font-bold mt-4 max-w-xl">Understand UTN’s international research partnerships.</h1></div><p className="text-teal-100">Data from Scopus · Reviewed by UTN</p></section><section className="flex items-center justify-center p-6"><form onSubmit={submit} className="card p-8 w-full max-w-md"><p className="text-sm font-bold text-utn-teal">WELCOME BACK</p><h2 className="text-3xl font-bold mt-2 mb-7">Sign in</h2><label className="label">Email<input className="input mt-2 mb-4" name="email" type="email" autoComplete="email" required/></label><label className="label">Password<input className="input mt-2 mb-2" name="password" type="password" autoComplete="current-password" required/></label><button type="button" className="text-sm text-utn-teal mb-5">Forgotten password?</button>{error&&<p role="alert" className="bg-red-50 text-red-700 p-3 rounded-lg mb-4">{error}</p>}<button disabled={busy} className="btn w-full">{busy?"Signing in…":"Sign in"}</button></form></section></main>}
