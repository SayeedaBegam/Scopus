"use client";import {FormEvent,useEffect,useState} from "react";import {api} from "@/lib/api";type D={id:number;report_scope:string;professor_id?:number;reason:string;status:string;reviewer_note?:string};
export default function Downloads(){const [items,setItems]=useState<D[]>([]),[message,setMessage]=useState("");const load=()=>api<D[]>("/download-requests").then(setItems);useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api("/download-requests",{method:"POST",body:JSON.stringify({report_scope:f.get("report_scope"),professor_id:f.get("professor_id")?Number(f.get("professor_id")):null,filters_json:{},reason:f.get("reason")})});e.currentTarget.reset();setMessage("Your request was sent to an administrator.");load()}return <><h1 className="text-3xl font-bold">Download access</h1><p className="text-slate-500 mt-2">Request a filtered report or review pending requests if you are an administrator.</p><form onSubmit={submit} className="card p-6 mt-6 grid md:grid-cols-3 gap-4"><label className="label">Report<select className="input mt-1" name="report_scope"><option value="overall">Overall UTN report</option><option value="professor">Professor report</option></select></label><label className="label">Professor ID (if applicable)<input className="input mt-1" name="professor_id" type="number"/></label><label className="label">Reason<input className="input mt-1" name="reason" required minLength={3}/></label><button className="btn w-fit">Request download access</button></form>{message&&<p className="mt-4">{message}</p>}<div className="card overflow-auto mt-6"><table className="table"><thead><tr><th>Report</th><th>Reason</th><th>Status</th><th>Admin note</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.report_scope}</td><td>{x.reason}</td><td><span className="badge">{x.status}</span></td><td>{x.reviewer_note||"—"}</td></tr>)}</tbody></table></div></>}
