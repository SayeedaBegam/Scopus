# Shared project material

`source-data/` contains the original UTN instructions and input CSV supplied with the project. Neither application writes to this directory.

- The legacy Streamlit application accepts the CSV through its upload screen.
- The new platform includes a separate sanitized example under `platform/samples/`.
